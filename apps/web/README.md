# Family Tasks — Web

App Next.js do produto **Family Tasks**.

Visão de produto, domínio e protocolo de agentes: veja o [README na raiz do monorepo](../../README.md) e `docs/`.

## Scripts

```bash
cd apps/web
cp .env.example .env.local   # preencha Supabase
npm install
npm run dev
```

Não documentamos instalação longa aqui — o README raiz é o da ideia do produto.
