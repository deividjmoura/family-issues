/** Face Executor: tema game-like. Epic 7 (#7). */
export default function ExecutorLayout({ children }: { children: React.ReactNode }) {
  return <div className="theme-game min-h-svh">{children}</div>;
}
