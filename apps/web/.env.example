# Copie para apps/web/.env.local e preencha (Supabase → Project Settings → API).
# Nunca commitar valores reais.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
# Só servidor. Ignora RLS — usar com parcimônia.
SUPABASE_SERVICE_ROLE_KEY=
